# mean ,sum ,mix, std, var

import numpy as np

arr = np.array([10,20,30,40,10,40])
              

print(np.sum(arr))
print(np.mean(arr))
print(np.min(arr))
print(np.max(arr))
print(np.median(arr))